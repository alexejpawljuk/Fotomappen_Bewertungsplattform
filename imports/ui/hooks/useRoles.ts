// import { useTracker } from 'meteor/react-meteor-data';
// import { Meteor } from 'meteor/meteor';

// export function useRoles(): string[] {
//     return useTracker(() => (Meteor.user()?.roles as string[]) ?? [], []);
// }